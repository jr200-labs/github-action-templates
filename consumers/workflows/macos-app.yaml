# GENERATED/SHARED WORKFLOW: copied into consuming repos by sync-shared.
# Do not edit in consuming repos; change jr200-labs/github-action-templates and sync.
name: macos-app

on:
  pull_request:
  workflow_dispatch:
  repository_dispatch:
    types: [release-published]

permissions:
  contents: write

concurrency:
  group: macos-app-${{ github.event.pull_request.number || github.event.client_payload['release-tag'] || github.ref }}
  cancel-in-progress: false

jobs:
  app:
    uses: jr200-labs/github-action-templates/.github/workflows/build_macos_app.yaml@master
    with:
      release-tag: ${{ github.event.client_payload['release-tag'] || '' }}
      publish: ${{ github.event_name == 'repository_dispatch' }}
      runner: ${{ fromJSON(vars.RUNNER_PROFILES)[vars.RUNNER_PROFILE].macos }}
      publish-runner: ${{ fromJSON(vars.RUNNER_PROFILES)[vars.RUNNER_PROFILE].default }}
