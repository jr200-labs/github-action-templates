# Check app packaging in CI. Release Please creates a draft; the publisher
# exposes it only after every verified update asset has been attached.
includes:
  - macos-app
  - publish-macos-app
files:
  - .shared/package-macos-app.py
